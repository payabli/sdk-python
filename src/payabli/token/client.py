# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.payabli_access_token_response import PayabliAccessTokenResponse
from .raw_client import AsyncRawTokenClient, RawTokenClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class TokenClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTokenClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTokenClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTokenClient
        """
        return self._raw_client

    def create_server_side_token(
        self,
        *,
        client_id: str,
        client_secret: str,
        state: typing.Optional[str] = OMIT,
        permissions: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PayabliAccessTokenResponse:
        """
        Exchanges a client ID and client secret for a short-lived Bearer access token using the OAuth2 client-credentials flow. Designed for server-to-server use: the credentials and the returned token stay on your backend. Send the returned `access_token` in the `Authorization` header as `Bearer <access_token>` on subsequent API calls. See the [OAuth authentication guide](/developers/oauth-authentication) for the full flow.

        Parameters
        ----------
        client_id : str
            The client ID issued for your integration when credentials are provisioned in the Payabli Portal.

        client_secret : str
            The client secret issued alongside the client ID. Keep it on your backend and never expose it in client-side code.

        state : typing.Optional[str]
            An optional opaque value echoed back in the response. Use it to correlate the request with its response.

        permissions : typing.Optional[typing.Sequence[str]]
            An optional array of permission IDs that scopes the token to a subset of the credential's granted permissions. When omitted, the token carries all permissions granted to the credential.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayabliAccessTokenResponse
            Access token issued.

        Examples
        --------
        from payabli import payabli

        client = payabli(
            api_key="YOUR_API_KEY",
        )
        client.token.create_server_side_token(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        """
        _response = self._raw_client.create_server_side_token(
            client_id=client_id,
            client_secret=client_secret,
            state=state,
            permissions=permissions,
            request_options=request_options,
        )
        return _response.data


class AsyncTokenClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTokenClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTokenClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTokenClient
        """
        return self._raw_client

    async def create_server_side_token(
        self,
        *,
        client_id: str,
        client_secret: str,
        state: typing.Optional[str] = OMIT,
        permissions: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PayabliAccessTokenResponse:
        """
        Exchanges a client ID and client secret for a short-lived Bearer access token using the OAuth2 client-credentials flow. Designed for server-to-server use: the credentials and the returned token stay on your backend. Send the returned `access_token` in the `Authorization` header as `Bearer <access_token>` on subsequent API calls. See the [OAuth authentication guide](/developers/oauth-authentication) for the full flow.

        Parameters
        ----------
        client_id : str
            The client ID issued for your integration when credentials are provisioned in the Payabli Portal.

        client_secret : str
            The client secret issued alongside the client ID. Keep it on your backend and never expose it in client-side code.

        state : typing.Optional[str]
            An optional opaque value echoed back in the response. Use it to correlate the request with its response.

        permissions : typing.Optional[typing.Sequence[str]]
            An optional array of permission IDs that scopes the token to a subset of the credential's granted permissions. When omitted, the token carries all permissions granted to the credential.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayabliAccessTokenResponse
            Access token issued.

        Examples
        --------
        import asyncio

        from payabli import Asyncpayabli

        client = Asyncpayabli(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.token.create_server_side_token(
                client_id="YOUR_CLIENT_ID",
                client_secret="YOUR_CLIENT_SECRET",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_server_side_token(
            client_id=client_id,
            client_secret=client_secret,
            state=state,
            permissions=permissions,
            request_options=request_options,
        )
        return _response.data
